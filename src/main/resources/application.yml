spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ref_select?useUnicode=true&charAacterEncoding=utf8&serverTimezone=UTC
    username: sa
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080
